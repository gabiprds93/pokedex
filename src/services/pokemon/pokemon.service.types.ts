// Pokemon service interfaces and types

/** Params of endpoint.
 * @interface Params
 * @since 0.1.0
 * @property {number} offset The offset param of endpoint.
 * @property {number} limit The limit param of endpoint.
 */
export interface Params {
  offset: number;
  limit: number;
}
